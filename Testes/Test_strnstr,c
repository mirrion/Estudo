#include <string.h>
#include <stdio.h>

int main(void)
{
	const char *largestring = "Foo Bar Bar Baz";
	   const char *smallstring = "Bar";
	   char	*ptr;

	   ptr = ft_strnstr(largestring, smallstring, 9);

	   printf(" \n%s", ptr);
}
